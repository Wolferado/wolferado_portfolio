import '../../styles/global.css';

const Contacts = () => {
    return (
        <>
            <div id="section-heading">
                <h1>Contacts</h1>
            </div>
        </>
    );
}

export default Contacts;